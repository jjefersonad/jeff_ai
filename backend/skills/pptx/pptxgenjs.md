# ⚠️ Documento legado — não reflete o estado atual da skill

Este arquivo (`pptxgenjs.md`) documenta o uso de **pptxgenjs/Node** para
criar `.pptx` do zero. Ele foi **aposentado** pela change
`custom-office-doc-tools` em favor da tool nativa `create_pptx_presentation`
(`python-pptx`).

Veja o `SKILL.md` atualizado para o caminho suportado.
