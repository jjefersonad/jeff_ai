# ⚠️ Documento legado — não reflete o estado atual da skill

Este arquivo (`editing.md`) documenta o fluxo original de **edição** de
`.pptx` baseado em `soffice`/`pptxgenjs`/`markitdown`/`unpack`+`pack`. Ele foi
**aposentado** pela change `custom-office-doc-tools`.

A skill `pptx` agora cobre apenas **criação** de apresentações via a tool
nativa `create_pptx_presentation` (python-pptx). **Edição de `.pptx` existente
está fora do escopo** desta entrega.

Veja o `SKILL.md` atualizado para o caminho suportado.
