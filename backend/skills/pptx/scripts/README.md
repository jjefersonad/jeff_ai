# Scripts legados — todos aposentados

A pasta `office/` e os arquivos soltos desta pasta (`clean.py`, `add_slide.py`,
`thumbnail.py`) eram o caminho original baseado em **soffice/LibreOffice/
pptxgenjs/markitdown**. Foram aposentados pela change `custom-office-doc-tools`.

Para gerar apresentações, use a tool nativa (registrada em
`src/agents/assistant/agent.py` e `src/agents/requirements_specialist.py`):

- `create_pptx_presentation` — `.pptx` via python-pptx

Detalhes em `../SKILL.md` e em `office/README.md`.
